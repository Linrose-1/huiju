# miniapp

会聚微信小程序，技术栈为 uni-app + Vue 3 + Vite + TypeScript + Pinia。

本目录由原 HBuilderX `frontend/` 工程迁移而来：原有 `static` 和 `uni_modules` 已保留在 `src/`，入口已转换为 Vue 3 CLI 工程结构。

首次运行前需要在仓库根目录安装依赖，并在 `src/manifest.json` 填写实际微信小程序 AppID。
