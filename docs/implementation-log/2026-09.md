# 2026-09 实现日志

## 2026-09-25｜工程骨架与协作基线

- 范围：项目目录、技术文档入口和协作治理文件。
- 改动：建立 pnpm workspace 及 miniapp、admin、api、packages 骨架；将原 HBuilderX 前端内容归入 `apps/miniapp/src`；新增根与应用级 `AGENTS.md`、当前 `HANDOFF.md`、运行时版本约束、忽略规则和记录入口。
- 验证：静态检查根与应用 `package.json`、workspace 配置、关键目录和文档路径可读取；本机 Node `v24.14.0` 与声明一致。
- 未验证：未安装依赖、未生成锁文件、未运行 type-check、测试、构建、服务、微信开发者工具或真机验收。本机 pnpm `9.5.0` 与项目声明的 `12.6.0` 不一致。
- 关联文档：`docs/architecture/技术选型与项目结构.md`、根 `AGENTS.md`、根 `HANDOFF.md`。

## 2026-09-25｜首次依赖安装与三端构建

- 范围：workspace 依赖、miniapp 兼容版本、API 模块格式、三端工程验证。
- 改动：通过 Corepack pnpm `12.6.0` 首次安装并生成锁文件；配置依赖构建脚本白名单；固定小程序 Vue `3.4.21`、`@dcloudio/types` `3.4.31` 和 VueUse `11.3.0`；API 切换为 ESM/NodeNext 并新增 `tsconfig.build.json`；补充管理后台 Vite 类型入口；排除小程序第三方 `uni_modules` 的源码级类型检查。
- 验证：锁文件通过 pnpm 供应链策略检查；`pnpm peers check` 无问题；根 `type-check` 通过；根 `build` 通过；API 独立重建后存在与启动脚本一致的 `apps/api/dist/main.js`。
- 未验证：未运行 lint、自动化测试、服务进程、数据库、浏览器、HBuilderX、微信开发者工具、真机、部署或生产验收。
- 已知告警：管理后台首个 JS chunk 约 1 MB；uni-scss 使用将被未来 Sass 移除的旧语法；小程序 AppID 尚未配置。
- 关联文档：根 `HANDOFF.md`、`docs/开发踩坑与防复发.md`。

## 2026-09-25｜关联 GitHub 仓库

- 范围：本地 Git 元数据和远程地址。
- 改动：初始化本地 Git，默认分支为 `main`；将 `origin` 设置为 `https://github.com/Linrose-1/huiju.git`。
- 验证：`git remote -v` 的 fetch/push 地址一致；远程当前无分支或标签；本地尚无 commit。
- 未验证：尚未暂存、提交或推送任何文件。
- 关联文档：根 `HANDOFF.md`。

## 2026-09-25｜主数据库切换为 MySQL

- 范围：数据库技术基线、API 数据库适配、依赖和工程文档。
- 改动：将主数据库由 PostgreSQL 改为 MySQL；移除 `pg` 与 `@types/pg`，增加 `mysql2`；Drizzle dialect 改为 `mysql`；数据库连接改用 mysql2 Promise 连接池；示例连接串、协作规则和架构文档同步更新；关闭 pnpm 隐式 peer 安装并显式声明 uni-automator 所需 Jest peer；新增 ADR-0001。
- 验证：依赖安装成功；锁文件供应链策略检查通过；`pg` 驱动不存在且 `mysql2` 为唯一关系数据库驱动；peer dependency 无问题；根 `type-check` 和根 `build` 通过，最终依赖裁剪后再次通过根 `type-check` 和 API build。
- 未验证：尚未启动真实 MySQL，未运行 migration，也未验证连接、事务、锁或并发报名行为。
- 关联文档：`docs/adr/0001-primary-database-mysql.md`、`docs/architecture/database.md`、根 `HANDOFF.md`。

## 2026-09-25｜工程检查、OpenAPI 客户端与本地数据库基线

- 范围：lint、环境示例、MySQL 本地版本与迁移命令、健康接口契约与生成客户端。
- 改动：对 uni-app 页面入口应用贴合框架的 lint 例外；根 TypeScript 5.9.3 满足 `openapi-typescript` peer，API 与管理后台保留各自 TypeScript 6；固定本地 MySQL 8.4.11 Compose 镜像；增加环境变量说明、Drizzle 生成/检查/迁移命令；从 NestJS 实际路由导出 OpenAPI 并生成客户端；为健康接口增加响应 DTO 和真实 HTTP 客户端测试。
- 验证：`corepack pnpm peers check` 无 peer 问题；`docker-compose --env-file .env.example -f infra/docker/compose.yaml config --quiet` 通过；`corepack pnpm api:generate` 生成 `/api/v1/health` 契约与类型；`corepack pnpm verify` 的 lint、三端类型检查、API 1 个 HTTP 测试与三端构建均通过。后台尚无测试，测试命令明确允许无测试文件退出。
- 未验证：未启动 MySQL、运行迁移或检查真实数据库事务；未连接真实微信、管理后台浏览器、小程序开发者工具或真机。健康接口不依赖数据库，不能代表业务流程跑通。
- 已知告警：管理后台单个 JS chunk 约 1 MB；uni-scss 存在 Sass 弃用提示；小程序 AppID 尚未配置。
- 关联文档：`docs/architecture/engineering-baseline.md`、`docs/api/README.md`、根 `HANDOFF.md`。

## 2026-09-25｜收费活动与特殊状态规则确认

- 范围：收费活动、活动咨询联系方式、再次报名、活动取消人数口径及答案锁定。
- 已确认：收费活动使用单一人民币固定金额并在首个报名后锁定；报名立即占位，平台不参与或记录付款退款。免费和收费活动均填写咨询联系方式，曾成功报名者持续拥有查看权。本人取消后可按当前条件再次报名；活动取消保留取消前名单与人数；答案在取消后永久锁定、下架期间暂时锁定。
- 文档：同步 `docs/product/首期PRD.md`、需求草案、小程序导航，以及 `docs/architecture/database.md`、`permissions.md`、`first-flow.md`；清理对应待确认表述。
- 验证：静态检查上述文档关键词与本地链接；本次未修改业务代码，未生成 schema 或运行 migration。

## 2026-09-25｜首批 MySQL Schema 与待执行迁移

- 范围：身份会话、会员与首次邀请、活动、报名、答案、操作历史及站内通知。
- 改动：在 `apps/api/src/database/schema` 建立 13 张 Drizzle 表；加入微信身份和邀请唯一约束、活动时间/容量/收费约束、每人每活动唯一报名、报名状态与签到一致性约束、操作历史及通知索引。生成业务建表（含报名一致性约束）和平台根节点种子两步迁移。
- 验证：Drizzle `db:check` 通过并识别 13 张表；根 `verify` 通过 lint、三端类型检查、API 1 个 HTTP 测试和三端构建。
- 未验证：未连接或启动 MySQL，未执行 migration，未证明外键、检查约束、事务锁和并发名额在真实数据库生效。
- 已知告警：管理后台大 chunk 与 uni-scss Sass 弃用提示保持不变；小程序仍未配置 AppID。

## 2026-09-25｜首批迁移审查问题修正

- 范围：首批迁移时间精度、收费金额约束、活动取消人数口径及后台目录规范。
- 改动：将 `updated_at` 改为由 Drizzle 更新语句显式写入，重新生成不含错误 `ON UPDATE CURRENT_TIMESTAMP` 的首批迁移；收费分支显式要求金额非空且大于零；活动增加取消时报名人数快照并在取消状态约束中要求该快照；后台目标目录统一为 `modules/<domain>`；增加迁移 SQL 契约测试。
- 验证：迁移契约测试通过，静态确认所有 `timestamp(3)` 默认值均为 `CURRENT_TIMESTAMP(3)`、迁移不再生成数据库端 `ON UPDATE`、收费条件包含 `IS NOT NULL` 且取消快照进入建表约束；Drizzle `db:check`、根 `verify`、peer dependency 检查和 Compose 静态检查均通过。根 `verify` 包含 lint、三端类型检查、2 个 API 测试及三端构建。
- 未验证：尚未连接或启动 MySQL，未执行 migration，未在真实 MySQL 中验证空收费金额拒绝、取消快照写入或并发事务行为。

## 2026-09-25｜MySQL 首批迁移与约束实测

- 范围：隔离空白 MySQL 8.4.11 实例、两步首批迁移、收费金额、容量和取消人数快照约束。
- 环境：使用独立 Compose 项目 `huiju-validation-20260925` 和本机端口 `33317`；没有使用或覆盖既有数据库。验收后停止容器，保留专用验证卷。
- 验证：首次迁移成功，实际生成 13 张业务表和 1 张 Drizzle 迁移表，迁移记录为 2 条，平台根节点为 1 条；重复运行迁移后记录数和根节点数保持不变。7 个 `updated_at` 列均为毫秒精度且默认值为 `CURRENT_TIMESTAMP(3)`。
- 约束结果：MySQL 实际拒绝收费金额 `NULL`、收费金额 `0`、当前人数超过容量和取消活动缺少人数快照；接受正数收费金额和完整取消快照。当前人数从 10 更新为 8 后，取消人数快照保持 10。
- 边界：当前只证明迁移和数据库静态约束生效；快照不可变仍由后续业务服务控制，尚未实现报名事务，也未进行并发名额、共享环境或生产验证。

## 2026-09-25｜主机 3306 开发库初始化

- 范围：用户提供的本地主机 `127.0.0.1:3306/huiju`；账号凭据仅在当前进程环境中使用，未写入项目文件。
- 环境核对：数据库初始为空，服务实际版本为 MySQL 8.0.36，与项目固定的 MySQL 8.4.11 交付基线不同。
- 迁移结果：两步迁移首次执行和重复执行均成功；库内现有 13 张业务表、1 张 Drizzle 迁移表、2 条迁移记录和 1 个平台根节点。
- 约束结果：在事务中验证收费金额 `NULL`、收费金额 `0`、容量超限和取消缺少人数快照均被拒绝；正数收费金额和完整取消快照成功，当前人数从 10 改为 8 后快照保持 10。事务已回滚，验证会员数据为 0 条。
- 边界：当前主机开发库可以继续用于开发，但不能代替 MySQL 8.4.11 发布基线验证；报名服务和并发事务尚未实现。

## 2026-09-25｜Git 首次提交与推送

- 范围：当前工程骨架、首批迁移、产品与架构文档以及高保真设计稿。
- 提交：创建根提交 `048be68`（`chore: initialize Huiju workspace`），共纳入 417 个文件，并推送到 `origin/main`。
- 提交前验证：凭据扫描未发现用户提供的数据库密码；`.env`、依赖目录和构建产物均被忽略；`corepack pnpm verify` 通过 lint、三端类型检查、2 个 API 测试和三端构建。
- 已知告警：HBuilderX 导入的 `uni_modules` 保留上游尾随空格；小程序构建仍有 uni-scss Sass 弃用提示，管理后台仍有大 chunk 提示。
