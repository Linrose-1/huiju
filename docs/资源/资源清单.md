# 首个真实流程资源清单

更新时间：2026-09-26。本文只记录素材来源与已知差异，不代表微信开发者工具或真机视觉验收已通过。

## 原稿与生成资源

已逐张查看 `设计稿/会聚-登录页-高保真.png`、`会聚-活动Tab-高保真.png`、`会聚-助手Tab-高保真.png`、`会聚-我的Tab-高保真.png`；四张原稿另存本目录 `原始图片/`，便于素材交接。原稿是完整页面参考，不能作为页面运行图片。

仓库已有 static 中的 uni、logo、c1–c9 等为工程示例资源，没有适合本次园区/助手背景的可复用独立素材。以下三张由内置 imagegen 生成，无真实用户资料、凭据或隐私输入；不是实际活动的照片或活动记录。

| 页面/用途 | 原图 | 运行路径（相对仓库根） | 像素尺寸 | 运行大小 |
| --- | --- | --- | --- | --- |
| 登录背景；活动/助手/我的头图 | `生成素材/campus.png` | `apps/miniapp/src/static/huiju/campus.jpg` | 1024×1536 | 376821 bytes |
| 助手“帮我发活动”卡片背景 | `生成素材/assistant-plan.png` | `apps/miniapp/src/static/huiju/assistant-plan.jpg` | 1536×1024 | 76562 bytes |
| 助手“帮我找活动”卡片背景 | `生成素材/assistant-find.png` | `apps/miniapp/src/static/huiju/assistant-find.jpg` | 1536×1024 | 115914 bytes |

运行资源仅使用本机 System.Drawing 进行 PNG → JPEG 格式转换（质量 80、尺寸不变），不做图像内容编辑。三个 JPEG 合计 569297 bytes，页面按显示容器裁切。不接 CDN 或对象存储。

## 明确差异与人工资源需求

- 园区图：保留玻璃建筑、绿树、暖日光、石墙的视觉主题，但不是设计稿原始建筑图片，树木、石墙纹理和构图不同；图内不生成石墙文字。若要求图片本身逐像素一致，需要提供设计稿独立背景原图。
- 三个 Tab 头图暂共用园区图。设计稿对应横向构图并不完全相同，共用竖图裁切仍需微信工具逐页检查。
- 筹备图：台历、笔记本位置与稿件近似，未烘焙活动标题、日期和清单文字，防止与可编辑业务信息混淆。原稿的静物角度、植物、页纸不同。
- 发现图：保留金绿指南针、园区背景、左侧留白；指南针为独立生成，细节与原稿不同，不包含稿内兴趣气泡文字。该图不表示 AI 服务已经可用。
- 不生成或复制设计稿示例人物头像、报名者头像、活动封面、活动名称/人数等业务数据。真实头像和真实活动内容必须来自对应业务数据。

## 图标与缺失清单

页面普通图标优先使用仓库已有 `apps/miniapp/src/uni_modules/uni-icons/components/uni-icons/uniicons.ttf`。底部导航需 PNG，由页面实现工作者从该字体导出：

| 文件（`apps/miniapp/src/static/huiju/`） | 字体图标 | 用途 |
| --- | --- | --- |
| `tab-activity-normal.png` | calendar | 活动 Tab 未选中 |
| `tab-activity-selected.png` | calendar-filled | 活动 Tab 选中 |
| `tab-mine-normal.png` | person | 我的 Tab 未选中 |
| `tab-mine-selected.png` | person-filled | 我的 Tab 选中 |
| `tab-assistant-placeholder.png` | 无，透明占位 | 助手 Tab，等待四芒星原图 |

需要人工寻找的资源：

| 页面 | 用途及稿中位置 | 建议交付尺寸/格式 | 当前处理 |
| --- | --- | --- | --- |
| 活动/助手/我的 | 底部导航中间“助手”四芒星：灰色空心未选中、深绿实心选中 | 两个透明 PNG，81×81 px；可附 SVG 源文件 | 81×81 透明布局占位，保留助手文字，不用 emoji 或其他不符图标 |
| 登录 | 邀请来源浅绿胶囊左侧双叶图标 | 透明 PNG 64×64 px，或 SVG 源文件 | 尚未取得独立原稿；邀请提示使用真实来源状态文字 |
| 登录 | 主品牌“会聚”字标 | 透明 PNG 建议 600×300 px，或 SVG；如交付字体需有使用授权 | 当前使用系统宋体文字，不能声明品牌字形与原稿一致 |
| 四页 | 如要求与原稿完全一致的品牌图标/插画 | 原始 SVG 或透明 PNG，按设计导出 2×/3× | uni-icons 语义匹配不等于原稿形状完全相同，待截图对照确认剩余差异 |

其他当前图标映射：我的名片 `contact-filled`；助手功能 `compose` / `checkbox` / `search` / `paperplane`。文档、四宫格和时钟使用空位，详见后补清单；登录双叶未绘制。Tab PNG 已读取确认全部 81×81 px，助手图标预期约 24px 显示。

下一步：在微信开发者工具检查背景裁切和图标对齐；取得助手四芒星、登录双叶及品牌字标原图后补齐对应资源，并继续保留真实服务未开放状态。


## 开发者工具截图后补充（2026-09-26）

截图发现当前 uni-icons 实际没有 paper、grid、clock 三个名称，已停止使用无效图标名，并保留固定大小的空位，保证文字对齐。以下需人工补齐：

| 页面 | 用途/稿位置 | 显示尺寸；建议资源 | 当前处理 |
| --- | --- | --- | --- |
| 助手 | 第一卡“智能生成活动方案”左侧文档轮廓 | 23×23 CSS px；69×69透明PNG或SVG | 固定23px空位 |
| 助手 | 第二卡“推荐优质园区活动”左侧四宫格 | 23×23 CSS px；69×69透明PNG或SVG | 固定23px空位 |
| 活动详情 | 活动时间左侧时钟 | 21×21 CSS px；63×63透明PNG或SVG | 固定21px空位 |
| 报名表/本人报名详情 | 活动摘要时间左侧时钟 | 18×18 CSS px；54×54透明PNG或SVG | 固定18px空位 |
| 本人报名详情 | 报名时间左侧时钟 | 19×19 CSS px；57×57透明PNG或SVG | 固定19px空位 |

当前截图见 `验收截图/`。四个主页面仅核对当前真实空状态/未登录/不可用状态，不能据此称完整业务数据、登录授权、全部屏幕或真机视觉验收通过。背景和字标差异仍保留。


## 2026-09-26 开发收尾对照

- 已再次逐张查看四主页面及活动详情、报名表、本人报名详情原稿；原生标题、真实身份/数据、不可用能力文案继续优先于原稿示例。
- 报名表：单选四列、多选三列等宽；补手机号/简答清除按钮、长文本计数、温馨提示图标和输入光标避让。清除/info-filled来自已核实的uni-icons。
- 活动列表和报名摘要：显示完整开始/结束时间，同日简写结束时刻，跨日保留两端日期；详情摘要区分封面高度，保留收费金额和本人报名状态。
- 本人报名详情：取消、下架改为中性/警示状态，不继续显示绿色成功和到场提示。
- 明确业务差异：原稿长文本示例显示500，本接口现有上限为5000，页面按接口显示5000；原稿多选“最多3项”没有正式产品/接口限制，不擅自添加。正式咨询字段为自由文本，不伪造电话号码/微信号按钮。
- 本次为设计稿对照后的代码修正、27项自动化测试、类型检查和微信目标构建证据；登录后页面截图、多屏/键盘与真机仍按用户要求集中后置。不能据此声明像素级视觉验收通过。
