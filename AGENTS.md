# 会聚项目协作规则

本文件约束本仓库中的人工与智能代理协作。子目录中的 `AGENTS.md` 可补充应用专属规则；冲突时以更接近目标文件的规则为准，但不得突破产品事实、数据安全和授权边界。

## 1. 事实来源

按以下顺序判断当前事实：

1. `docs/product/首期PRD.md`：首期产品范围与验收口径。
2. `docs/product/小程序框架与导航.md`：小程序信息架构与页面入口。
3. `docs/architecture/`：技术选型、数据、身份与权限设计。
4. 已明确确认的 ADR、接口契约和设计稿。
5. 当前代码、配置和可重复的运行证据。

`HANDOFF.md`、实现日志、计划和聊天记录只记录状态或过程，不得覆盖上述正式来源。发现文档、代码和运行结果矛盾时，先标明矛盾，再修正对应的正式来源或实现。

## 2. 产品与业务边界

- 会聚是独立产品。未经明确确认，不接入猩聚社，也不共享其账号、业务数据或权限体系。
- 尚未确认的业务细节保持“待确认”，不得用实现便利替用户补定产品规则。
- 受限操作必须由服务端校验身份、资料完整度和权限；隐藏按钮不能代替鉴权。
- 用户完成手机号绑定、头像和姓名等必要资料后，才能执行要求完整身份的操作。具体字段和触发点以 PRD 为准。
- 邀请关系采用首次有效来源，建立后不得被普通重复访问覆盖。
- 活动支持免费和付费两种模式；首期不实现平台内支付，付费记录按正式产品规则处理。
- 会员资料按字段控制可见性，接口只返回当前调用者有权查看的数据。
- AI 只负责理解、推荐和草拟；产生业务后果的内容由用户确认。身份敏感字段不得无必要传给模型供应商。

## 3. 工程边界

- 使用 pnpm workspace 管理 TypeScript 单体仓库。
- `apps/miniapp` 是 uni-app 微信小程序；`apps/admin` 是 Web 管理后台；`apps/api` 是 NestJS 模块化单体。
- `packages/api-client` 只保存由 OpenAPI 契约生成或围绕契约封装的客户端。
- `packages/shared` 只保存真正跨端且稳定的常量、枚举和类型，不承载服务端业务规则。
- MySQL 是业务事实来源。客户端状态、缓存、搜索索引和 AI 上下文都不能替代数据库事实。
- 首期不预设微服务、Redis、消息队列、独立任务系统或 Outbox；只有出现已验证需求后再通过 ADR 引入。

## 4. 开始修改前

1. 阅读与任务直接相关的产品文档、架构文档和就近的 `AGENTS.md`。
2. 检查当前文件、Git 状态、依赖状态和可用运行证据。
3. 明确本次成功标准，只改实现目标所需的内容。
4. 保留不属于本次任务的改动和未知文件，不重置、不清理、不批量覆盖。

缺少的信息只在会实质改变范围、正确性或风险时询问。可逆的实现细节按现有约定作合理判断并继续。

## 5. 实现规则

- 优先最小、直接、可验证的实现，不为假设中的未来需求增加抽象层。
- 控制器和页面保持轻量；业务规则进入对应领域模块或服务。
- 模块不得绕过公开服务直接修改其他模块拥有的数据表。
- 跨模块状态变化使用明确的业务动作和事务边界，避免散落的字段更新。
- API 错误使用稳定的业务错误码和安全文案，不向客户端泄漏 ORM、SQL、堆栈或内部配置。
- 所有外部输入都在服务端验证；客户端验证只改善交互。
- 密钥、令牌、AppSecret、真实账号和生产连接串不得写入仓库。`.env.example` 只放变量名和安全占位值。
- 修改依赖版本时说明原因，并同步锁文件；没有安装授权时不生成或猜测锁文件。

## 6. 需要单独授权的操作

除非当前任务已经明确授权，否则不得执行：

- 初始化 Git、创建分支或 worktree、提交、推送、合并、重置或清理。
- 安装、升级或移除依赖。
- 执行数据库迁移、写入共享数据库或改变生产数据。
- 部署、发布、绑定外部账号或调用会产生费用的服务。
- 删除、移动或批量改写用户已有文件。

## 7. 验证与证据

验证力度与改动风险一致：

- L0 静态检查：文件、路径、JSON、配置和链接可读。
- L1 工程检查：类型检查、lint、单元测试、构建。
- L2 本地集成：真实本地 API、数据库、浏览器、HBuilderX 或微信开发者工具。
- L3 外部验收：真机、真实微信能力、第三方服务、部署环境或生产验证。

报告必须区分已通过、失败、部分通过、未知和未运行。静态检查不能表述为构建通过；H5 预览不能表述为微信真机通过；模拟数据不能表述为真实 API 或数据库通过；本地运行不能表述为已部署。

完成行为变更时优先运行能覆盖该风险的聚焦检查。没有依赖或环境时如实记录，不通过临时跳过类型、鉴权或校验来制造通过结果。

## 8. 文档职责

- `AGENTS.md`：稳定的协作和工程规则。
- `HANDOFF.md`：唯一一份当前交接索引，记录易变现状和下一步。
- `docs/implementation-log/YYYY-MM.md`：已经完成且有证据的实现记录。
- `docs/开发踩坑与防复发.md`：经过验证、可复用的失败模式与防复发规则。
- `docs/adr/`：重要且长期有效的架构决策。

功能、结构或约束发生变化时更新对应正式文档。不要把长期规则塞进 HANDOFF，也不要把未验证的猜测写成经验。

## 9. 完成标准

提交结果前确认：

- 每一处变更都能追溯到当前任务。
- 相关产品和架构边界未被悄悄改写。
- 已运行必要检查，并准确记录其层级和结果。
- 新增入口、脚本和文档之间的路径一致。
- 剩余风险和最小下一步清楚可执行。
