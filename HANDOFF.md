# 当前任务交接

- Task ID：`huiju-engineering-baseline-20260925`
- Project ID：`D:\code\gofor\会聚`
- 更新时间：2026-09-26 14:01 +08:00
- 状态：Current
- 下一工作项：首个业务切片——微信身份、首次邀请与资料补全。

> 本文件是唯一当前交接索引，不替代正式产品和架构来源。历史过程见月度实现日志；易变事实按各自观察时间重新核对。

## 1. 目标、范围与排除项

工程基础与审阅修正已完成，用户将开启新对话进行业务开发。本次收尾同步所有可入库工程文件和设计稿变更；不上传 `.env`、凭据、依赖、构建产物或本地数据库。不在收尾阶段新增业务功能。

首个可验证开发切片推荐“微信身份识别 → 首次邀请固定 → 资料补全 → 会话与受限操作门槛”，后续再衔接活动发布和报名。业务开发意图已明确；具体新对话授权以用户粘贴的任务为准。

## 2. 适用规则与正式来源

- 根 `AGENTS.md`，以及目标应用和 `packages/api-client/AGENTS.md`。
- 产品：`docs/product/首期PRD.md`、`小程序框架与导航.md`。
- 架构：`docs/architecture/技术选型与项目结构.md`、`auth.md`、`permissions.md`、`database.md`、`first-flow.md`。
- 工程：`docs/architecture/engineering-baseline.md`、`docs/testing/README.md`、`scripts/README.md`。
- 设计：`设计稿/` 当前文件和用户最新确认；截图中的视觉不覆盖正式产品权限或状态规则。

## 3. 当前进度

**已完成**

- pnpm workspace：uni-app 微信小程序、Vue 管理后台、NestJS API、共享包；MySQL + Drizzle。
- 首批 13 张表、三步迁移；微信身份三列使用 `utf8mb4_0900_bin`，应用连接池在每次借出前设置 UTC，失败拒绝借出。
- API 健康接口、OpenAPI 导出及生成客户端；三个小程序 Tab 与后台工程占位页。
- 小程序内容区重复标题已移除，页面标题仅由 `apps/miniapp/src/pages.json` 原生配置提供。
- `verify:plan` 只预览检查建议，`api:check` 用临时产物检查契约漂移而不覆盖仓库文件；完整 `verify` 已纳入检查。
- 借鉴猩球工坊与猩商态的任务导航、验证分层、副作用说明和环境隔离约束，取舍见 `docs/architecture/工程实践借鉴.md`。

**尚未实现**

- 微信登录/手机号授权、会话、邀请、会员资料、权限 Guard 和业务服务。
- 活动发布、列表/详情、报名与并发名额、签到、通知、后台业务与 AI。
- 客户端 API 环境隔离、迟到请求防护和微信传输适配目前是设计要求，不能称为已实现。
- 自动 CI、部署与生产环境；后台目前没有业务测试文件。

无未完成的修复进程；未把计划或占位页面作为可用产品。

## 4. 已确认决策与授权边界

- 会聚保持独立产品，未经授权不接入其他项目账号、业务数据或权限体系。
- 资料门槛为绑定手机号、主动设置头像和用户名称；默认资料不算完成，真实姓名不作为替代门槛。
- 首次有效邀请关系不能被后续访问覆盖；身份与受限操作由服务端判断。
- 活动免费/收费共用报名逻辑；收费仅展示固定金额，不记录或处理付款退款；咨询联系方式按历史报名关系授权。
- 取消再报名复用同一主记录、重新竞争名额；活动取消保存人数快照，答案及下架规则按 PRD。
- 保持现有技术栈，不预建微服务、Redis、队列、Outbox，不照搬参考项目业务体系。
- 本次用户授权全部可入库文件提交与远程同步；不授权后续自动提交/推送、创建分支/worktree、安装升级、其他数据库迁移或部署。
- 之前已授权并完成本机开发库三步迁移；该历史许可不扩展至未来迁移或其他环境。新对话遇到明确需另行授权的操作才询问，不重复确认已授权开发范围。

## 5. Git 与文件状态

- 观察时间：2026-09-26 14:01 +08:00，收尾提交前。
- 分支 `main`，远程 `origin` 为 `https://github.com/Linrose-1/huiju.git`。
- 已执行 `git fetch origin`，提交前 `HEAD...origin/main` 为 `0 0`；当时 HEAD 为 `aa33c91`。
- 本文件与工程修正、规则/检查工具、所有当前设计稿新增/修改/删除一并纳入本次同步。最终提交号及同步状态以新会话的 `git status`、`git log` 与 fetch 后比较为准，不把提交前快照当成最终状态。
- 已检查候选文件无本地数据库密码匹配或私钥标记；`.env` 被忽略。凭据不在日志、交接或提示词中传递。

## 6. 运行环境与外部系统

- 2026-09-26 本会话实测 Node `v24.14.0`、Corepack pnpm `12.6.0`；优先使用 `corepack pnpm`，新会话仍需重查版本。
- 2026-09-26 13:34 +08:00：`127.0.0.1:3306/huiju` 为 MySQL 8.0.36，三条迁移记录、1 个平台根节点、0 条微信身份；验证数据已回滚。根 `.env` 已由用户本地配置，不打印内容。
- 项目交付基线为 MySQL 8.4.11。首两步迁移在 2026-09-25 隔离 8.4.11 验证过；第三步身份修正只在本机 8.0.36 验证，8.4.11 待复验。
- 未为开发长期启动 API、后台或小程序服务；健康 HTTP 仅由测试进程短暂启动。当前端口/PID 未做全面盘点，启动前核对归属。
- 小程序 AppID 尚未配置；微信主体、接口权限、手机号授权、真实分享/登录、对象存储、AI 和部署账号均未核验。

## 7. 分层验证

**Passed**

- 2026-09-26 13:47：完整 `corepack pnpm verify` 通过 lint、三端类型检查、7 个脚本测试、6 个 API 测试、三端构建、Drizzle 静态检查和契约比较。
- 三个小程序编译页面原生标题正确，WXML 不含原内容区标题。
- 2026-09-26 13:34：真实 MySQL 8.0.36 的第三步迁移及重复运行通过；大小写不同身份分别保存/精确查询，完全重复身份仍被拒绝。
- 新建、复用和事务连接 UTC 验证通过，日期毫秒往返、数据库默认时间通过；临时测试事务全部回滚。
- 文档入口链接和 `git diff --check` 通过；正式契约、客户端生成文件与锁文件未被工程优化意外改写。

**Partial / Not Run / Unknown**

- 后台测试命令允许无测试文件退出，不能计为业务测试通过。
- 未实现登录/报名/权限，因此无业务闭环、真实微信、并发抢名额、浏览器或真机验收。
- 未验证第三步迁移在 MySQL 8.4.11 的行为，未部署。
- 当前无已知阻断性检查失败；保留 Sass 弃用、后台约 1 MB chunk、小程序 AppID 未配置提示。

## 8. 风险与不要重复的操作

- 不重新初始化 Git、生成首批表、重写已执行迁移或重复进行历史迁移修复。
- 不将“构建通过”“健康接口通过”表述为业务、微信或生产通过。
- 不覆盖当前设计稿；只实现本次明确范围，页面不手写页面标题。
- 不使用纯 Mock 证明数据库并发；测试必须标明替身、真实 HTTP、真实数据库的边界。
- 不把根 `.env`、账号、数据库密码或真实 token 放进代码、日志、截图或聊天。
- Corepack SOCKS5 代理历史问题及解决证据见 `docs/开发踩坑与防复发.md`；遇到问题先核对，不自动改系统代理或升级依赖。

## 9. 下一最小可验证动作

1. 读取上述规则与身份相关正式文档，核对 Git、依赖和微信配置是否具备。
2. 实现首个身份与资料切片：服务端微信适配、会话、首次邀请事务、本人资料接口及完整度判定，配套 OpenAPI/客户端与针对性测试。
3. 微信真实凭据缺失时继续可独立开发和隔离测试，但明确真实登录/手机号验收未完成；不要伪造或绕过身份门槛。头像存储等会实质影响范围的外部配置在需要时确认。
4. 再按确认设计稿接小程序登录/资料入口，原生标题与返回上下文保持一致；本切片验收后衔接活动和报名，不一次性铺开全部 PRD。

## 10. 接手必须重查

- Git 当前提交、工作区及与远程差异；不要仅相信本文件的收尾提交前快照。
- 根/子 `AGENTS.md`、实际 package 脚本、Node/pnpm 与安装依赖。
- `.env` 是否存在及连接目标（仅输出非敏感 host/port/database）；微信账号能力不可从环境变量名称推断为已验证。
- 数据库版本、迁移状态、服务端口与进程归属；涉及写入时核对当前授权。
- 当前设计稿文件与用户最新反馈；未确定产品细节保持待确认。
